import { PageLoading } from "@/components/shared/page-loading";

export default function AdminLoading() {
  return <PageLoading title="Loading admin..." message="Preparing marketplace controls and reporting." />;
}
