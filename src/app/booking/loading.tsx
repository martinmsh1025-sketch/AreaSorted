import { PageLoading } from "@/components/shared/page-loading";

export default function BookingLoading() {
  return <PageLoading title="Loading booking..." message="Fetching the latest booking status." />;
}
